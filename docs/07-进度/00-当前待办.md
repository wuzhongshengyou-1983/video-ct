# 当前待办 · v3 Phase 0

> 更新：2026-05-28 · 基于 `01-缺失项总览.md` 提取的可执行清单  
> 规则：完成一项打 ✅，不改其他内容

---

## Phase 0 — 本周可做（纯加法，¥0，不破坏现有功能）

### 数据库：5 张表 + 2 列

- [ ] 创建 `video_metrics` 表（真实 VMAF/MOS/SEI 帧级指标，Phase 0 核心）
- [ ] 创建 `benchmark_daily_stats` 表（头部博主日度快照统计）
- [ ] 创建 `account_entities` 表（账号实体，跨视频聚合用）
- [ ] 创建 `account_health_snapshots` 表（账号健康分快照，Phase 1 预建）
- [ ] 创建 `recurring_issues` 表（复发问题检测，Phase 2 预建）
- [ ] `ALTER TABLE diagnoses ADD COLUMN account_id UUID NULL` — 诊断关联账号
- [ ] `ALTER TABLE diagnoses ADD COLUMN diagnosis_sequence INT NULL DEFAULT 0` — 第几次诊断

### 后端端点

- [x] `POST /api/v1/events/track` — 行为事件收集（已交付 2026-05-28）
- [x] `POST /api/v1/diagnosis/{id}/resubmit` — 复诊入口（已交付 2026-05-28）
- [ ] `POST /api/v1/accounts` — 创建账号实体（关联多条视频）
- [ ] `GET /api/v1/accounts/{account_id}/diagnoses` — 账号下诊断历史列表

### 前端：3 处改动

- [x] `ReportActionPlan.vue` — 反馈按钮接入 `events/track`（已交付 2026-05-28）
- [ ] `DiagnoseSubmit.vue` — 新增「复诊」入口按钮（跳转 `/diagnose/resubmit/:id`）
- [ ] 新建路由 `/diagnose/resubmit/:id` + 页面（复用提交页，预填上次视频 URL）
- [ ] 首页或诊断列表 — 增加「绑定账号」快捷入口（可选，低优先）

---

## Phase 1 — 4-6 周后（前提：Phase 0 完成 + ≥500 条行为数据）

- [ ] `GET /api/v1/accounts/{id}/health` — 账号健康分计算接口
- [ ] H5 新路由 `/account/:id` — 账号个人中心页（改善曲线 + 健康分 + 对标分位）
- [ ] 集成 MediaCrawler — 批量采集同垂类头部博主视频，写入 `benchmarks` 表
- [ ] Admin 后台 — 账号健康分列表视图
- [ ] Consultant 端 — 账号改善轨迹可视化

---

## Phase 2 — 2-3 个月后（前提：Phase 1 数据积累）

- [ ] 复发问题自动检测（`recurring_issues` 表 + `DataSentinelAgent` 扩展）
- [ ] 改善建议闭环追踪（从「建议生成」到「执行」到「效果验证」）
- [ ] 实时诊断看板（WebSocket 全链路进度展示）

---

## 技术债（随时可做，不阻塞业务）

- [ ] `services/api/tests/test_api.py` — 补充真实测试用例（当前文件为空）
- [ ] `services/api/tests/test_agents.py` — 补充 Agent 单元测试（当前文件为空）
- [ ] 接入 Sentry 错误上报（`.env.example` 已预留但未实现）
- [ ] 补充 `infra/migrations/` 中 `video_metrics` 等新表的 Alembic 脚本

---

> 详细背景见 [`01-缺失项总览.md`](01-缺失项总览.md)  
> 版本发布记录见 [`/CHANGELOG.md`](../../CHANGELOG.md)
