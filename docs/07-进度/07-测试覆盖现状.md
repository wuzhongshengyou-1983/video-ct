# 测试覆盖现状

> 更新：2026-05-28 · 客观记录，不美化

---

## 总体评级：⚠️ 基础覆盖，核心路径空白

| 层级 | 文件 | 用例数 | 状态 |
|------|------|--------|------|
| E2E（Playwright） | `tests/e2e/api-health.spec.ts` | 4 | ✅ 可运行 |
| E2E（Playwright） | `tests/e2e/h5-critical-path.spec.ts` | 8 | ✅ 可运行 |
| E2E（Playwright） | `tests/e2e/payment-flow.spec.ts` | 9 | ✅ 可运行 |
| 后端单元/集成 | `services/api/tests/test_api.py` | 0 | ❌ 文件为空 |
| 后端 Agent 测试 | `services/api/tests/test_agents.py` | 0 | ❌ 文件为空 |

---

## E2E 覆盖详情

### api-health.spec.ts（4 个）
- `GET /healthz → 200`
- `GET /readyz → 200`
- `GET /docs → 200`（Swagger UI）
- `GET /api/v1/subscriptions/products → 200 + 返回数组`

### h5-critical-path.spec.ts（8 个）
- 首页加载 · 可见主标语
- 未登录访问 `/diagnose/submit` → 重定向到 `/login`
- 手机号 + OTP 完整登录流程
- 登录后访问 `/diagnose` · 诊断列表可见
- `/subscribe` 产品列表可见
- `/referrer` 分享官信息可见
- `/archive` 空档案引导可见
- `/me` 用户昵称 + 配额信息可见

### payment-flow.spec.ts（9 个）
- 产品列表查询
- 创建订单 + 返回支付参数
- 查询支付参数
- 查询订单状态（pending）
- 微信支付回调 Mock → 激活订阅
- 查询订单状态（paid）
- 查询当前有效订阅
- 取消订阅
- 重复回调幂等处理

---

## 未覆盖的高风险路径

| 路径 | 风险级别 | 说明 |
|------|---------|------|
| CT 诊断官 Agent 完整链路 | 🔴 高 | 诊断是核心业务，Agent 零测试 |
| 对标差距计算逻辑 | 🔴 高 | 数学运算 + LLM 解读，无单测 |
| Celery 异步任务成功/失败 | 🔴 高 | 任务队列状态转换无自动测试 |
| WebSocket 诊断进度推送 | 🟡 中 | 实时功能，未测 |
| 优惠券核销 + 边界条件 | 🟡 中 | 零单测，靠 E2E 间接覆盖 |
| 分享官二级裂变奖励计算 | 🟡 中 | 业务规则复杂，零测试 |
| Admin / Consultant 鉴权隔离 | 🟡 中 | RBAC 正确性未验证 |
| 人设 + 商业定位 Agent | 🟢 低 | 非核心路径，可后补 |

---

## 运行测试

```bash
# E2E 测试（需后端 + H5 先启动）
pnpm test:e2e

# 仅 API 健康检查（不依赖 H5）
npx playwright test api-health --config tests/e2e/playwright.config.ts

# 仅支付流程
npx playwright test payment-flow --config tests/e2e/playwright.config.ts

# 查看报告
npx playwright show-report playwright-report/
```

---

## 补测建议（按优先级）

1. **`test_agents.py`**：用 `pytest-asyncio` + Mock LLM 响应，测 `CTRadiologistAgent.run()` 的输出结构校验
2. **`test_api.py`**：用 `httpx.AsyncClient` + SQLite 内存 DB，测鉴权 / 诊断提交 / 订阅状态机
3. **Celery 任务测试**：`@pytest.mark.celery(always_eager=True)` 让任务同步执行再断言状态
